<h1>Articles</h1>

<p><%= link_to 'New Article', new_article_path %></p>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
      <small><%= article.created_at.to_s(:short) %></small>
    </li>
  <% end %>
</ul>
